<h1>Edit foodtruck booking</h1>
<%= simple_form_for ([@foodtruck, @booking]) do |f| %>
  <%= f.input :booking_status, :as => :select, :collection => Booking::STATUS %>
  <%= f.submit "Submit", class: "btn btn-primary" %>
<% end %>
